# 🔄 Анализ влияния миграций на Workflow VendHub OS

**Цель:** Понять, как каждая миграция повлияет на существующую логику, API и пользовательский опыт.

---

## 📊 МАТРИЦА ВЛИЯНИЯ

| Миграция | Backend | Frontend | Mobile | Bot | Workflow | Риск |
|----------|---------|----------|--------|-----|----------|------|
| CreateDictionariesTables | ➕ Новый модуль | ➕ Новые страницы | ❌ Нет | ❌ Нет | ❌ Нет | 🟢 |
| CreateDashboardWidgetsTable | ➕ Новый модуль | 🔄 Dashboard | ❌ Нет | ❌ Нет | ❌ Нет | 🟢 |
| CreateDailyStatsTable | ➕ CRON job | 🔄 Dashboard | ❌ Нет | ❌ Нет | ❌ Нет | 🟢 |
| CreateAnalyticsSnapshotsTable | ➕ CRON job | 🔄 Reports | ❌ Нет | ❌ Нет | ❌ Нет | 🟢 |
| CreateCustomReportsTable | ➕ Новый модуль | ➕ Report Builder | ❌ Нет | ❌ Нет | ❌ Нет | 🟢 |
| CreateOperatorRatingsTable | ➕ CRON job | ➕ Ratings page | ➕ Ratings | ➕ Notify | ❌ Нет | 🟢 |
| AddTaskRejectionFields | 🔄 TasksService | 🔄 Task forms | 🔄 Task flow | 🔄 Commands | 🔄 **Task workflow** | 🟠 |
| CreateInventoryReservationsTable | 🔄 InventoryService | 🔄 Inventory UI | ❌ Нет | ❌ Нет | 🔄 **Inventory workflow** | 🟡 |
| EnhanceAlertsSystem | 🔄 AlertsService | 🔄 Alerts UI | ➕ Push | 🔄 Alerts | 🔄 **Alert workflow** | 🟠 |
| CreateRouteStopsTable | 🔄 RoutesService | 🔄 Routes map | 🔄 Navigation | ❌ Нет | 🔄 **Routes workflow** | 🟡 |
| CreateIntelligentImportSystem | ➕ Новый модуль | ➕ Import wizard | ❌ Нет | ❌ Нет | ➕ **Import workflow** | 🔴 |
| CreateEquipmentManagementTables | 🔄 EquipmentService | 🔄 Equipment UI | ❌ Нет | ❌ Нет | 🔄 **Maintenance workflow** | 🔴 |

**Легенда:** ➕ Новое | 🔄 Изменение | ❌ Не затрагивает

---

## 🟢 БЕЗ ВЛИЯНИЯ НА WORKFLOW

### 1. CreateDictionariesTables

**Влияние на существующую логику:** НУЛЕВОЕ

```
ТЕКУЩИЙ WORKFLOW: Не изменится
┌─────────────────────────────────────────┐
│ Справочники хранятся в коде (enums)     │
│ или в отдельных таблицах               │
└─────────────────────────────────────────┘

НОВЫЙ WORKFLOW: Параллельная система
┌─────────────────────────────────────────┐
│ + Динамические справочники в БД         │
│ + UI для редактирования                │
│ + API для получения значений           │
│                                         │
│ Существующие enums продолжают работать │
└─────────────────────────────────────────┘
```

**Что добавится:**
- `GET /api/dictionaries` — список справочников
- `GET /api/dictionaries/:code/items` — элементы справочника
- `POST/PUT/DELETE` для управления

**Существующий код:** НЕ ТРЕБУЕТ ИЗМЕНЕНИЙ

---

### 2. CreateDashboardWidgetsTable

**Влияние на существующую логику:** НУЛЕВОЕ

```
ТЕКУЩИЙ DASHBOARD:
┌─────────────────────────────────────────┐
│ Фиксированные виджеты                   │
│ Одинаковый для всех пользователей      │
└─────────────────────────────────────────┘

НОВЫЙ DASHBOARD:
┌─────────────────────────────────────────┐
│ + Кастомизация виджетов (drag & drop)  │
│ + Сохранение позиций на пользователя   │
│ + Выбор типа графика                   │
│                                         │
│ Старый dashboard работает как раньше   │
└─────────────────────────────────────────┘
```

**Интеграция:** Опциональная, по feature flag
```typescript
// Можно включить постепенно
if (featureFlags.customDashboard) {
  return customWidgetsService.getUserWidgets(userId);
} else {
  return defaultDashboardConfig;
}
```

---

### 3. CreateDailyStatsTable + CreateAnalyticsSnapshotsTable

**Влияние на существующую логику:** НУЛЕВОЕ

```
ТЕКУЩАЯ АНАЛИТИКА:
┌─────────────────────────────────────────┐
│ Запросы в реальном времени             │
│ SELECT SUM(...) FROM transactions      │
│ Медленно на больших данных             │
└─────────────────────────────────────────┘

НОВАЯ АНАЛИТИКА:
┌─────────────────────────────────────────┐
│ + Предрассчитанные агрегаты            │
│ + CRON: каждую ночь собирает stats     │
│ + Быстрые запросы из daily_stats       │
│                                         │
│ Старые запросы работают как раньше     │
└─────────────────────────────────────────┘
```

**Новый CRON job:**
```typescript
@Cron('0 2 * * *') // Каждый день в 2:00
async aggregateDailyStats() {
  // Собирает статистику за вчера
  // НЕ влияет на существующие сервисы
}
```

---

### 4. CreateCustomReportsTable

**Влияние на существующую логику:** НУЛЕВОЕ

```
ТЕКУЩИЕ ОТЧЁТЫ:
┌─────────────────────────────────────────┐
│ Фиксированные отчёты в reports module  │
│ PDF/Excel генерация                    │
└─────────────────────────────────────────┘

НОВЫЕ ОТЧЁТЫ:
┌─────────────────────────────────────────┐
│ + Конструктор отчётов (Report Builder) │
│ + Сохранение конфигураций              │
│ + Расписание отправки по email         │
│                                         │
│ Существующие отчёты не меняются        │
└─────────────────────────────────────────┘
```

**Новый модуль:** `CustomReportsModule` — полностью изолирован

---

### 5. CreateOperatorRatingsTable

**Влияние на существующую логику:** МИНИМАЛЬНОЕ

```
ТЕКУЩИЙ WORKFLOW:
┌─────────────────────────────────────────┐
│ Нет системы рейтингов операторов       │
│ Оценка вручную менеджером              │
└─────────────────────────────────────────┘

НОВЫЙ WORKFLOW:
┌─────────────────────────────────────────┐
│ + Автоматический расчёт рейтинга       │
│ + CRON: еженедельно/ежемесячно         │
│ + Уведомления операторам               │
│ + Страница "Мой рейтинг" в mobile      │
│                                         │
│ Существующие процессы не меняются      │
└─────────────────────────────────────────┘
```

**Интеграция (опциональная):**
- TasksService: данные для расчёта (read-only)
- NotificationsService: отправка результатов
- Mobile app: новый экран

---

### 6. CreateRecipeSnapshotsTable

**Влияние на существующую логику:** НУЛЕВОЕ

```
ТЕКУЩИЕ РЕЦЕПТЫ:
┌─────────────────────────────────────────┐
│ Редактирование рецептов                │
│ Нет истории изменений                  │
└─────────────────────────────────────────┘

НОВЫЕ РЕЦЕПТЫ:
┌─────────────────────────────────────────┐
│ + При изменении создаётся snapshot     │
│ + История версий рецепта               │
│ + Откат к предыдущей версии            │
│                                         │
│ Текущее редактирование работает        │
└─────────────────────────────────────────┘
```

**Интеграция:** Хук в RecipesService
```typescript
// Добавить в существующий update метод
async updateRecipe(id, dto) {
  // Существующая логика...

  // + Создать snapshot (опционально)
  if (this.featureFlags.recipeSnapshots) {
    await this.snapshotService.createSnapshot(oldRecipe, 'update');
  }
}
```

---

### 7. AddPerformanceIndexes

**Влияние на существующую логику:** НУЛЕВОЕ (только ускорение)

```
СОЗДАНИЕ ИНДЕКСОВ:
┌─────────────────────────────────────────┐
│ CREATE INDEX CONCURRENTLY ...           │
│                                         │
│ • Не блокирует таблицы                 │
│ • Не меняет логику                     │
│ • Только ускоряет запросы              │
└─────────────────────────────────────────┘
```

---

## 🟡 МИНИМАЛЬНОЕ ВЛИЯНИЕ НА WORKFLOW

### 8. CreateInventoryReservationsTable

**Влияние:** Опциональное расширение inventory workflow

```
ТЕКУЩИЙ INVENTORY WORKFLOW:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Создать    │───▶│   Выполнить  │───▶│   Списать    │
│    задачу    │    │    задачу    │    │   товары     │
└──────────────┘    └──────────────┘    └──────────────┘
        │
        │ Проблема: товар может закончиться
        │ пока оператор едет
        ▼

НОВЫЙ INVENTORY WORKFLOW (опционально):
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Создать    │───▶│ Резервировать│───▶│   Выполнить  │───▶│   Списать    │
│    задачу    │    │    товар     │    │    задачу    │    │   резерв     │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
                           │
                           │ Товар заблокирован
                           │ для других задач
                           ▼
```

**Изменения в коде:**
```typescript
// TasksService - ОПЦИОНАЛЬНОЕ расширение
async createTask(dto: CreateTaskDto) {
  const task = await this.tasksRepo.save(dto);

  // + Резервирование (если включено)
  if (this.config.enableInventoryReservation && dto.items?.length) {
    await this.inventoryReservationService.reserve(task.id, dto.items);
  }

  return task;
}
```

**Можно включить через feature flag:**
```env
FEATURE_INVENTORY_RESERVATION=false  # По умолчанию выключено
```

---

### 9. CreateInventoryDifferenceThresholdsTable

**Влияние:** Расширение проверки инвентаря

```
ТЕКУЩАЯ ПРОВЕРКА:
┌─────────────────────────────────────────┐
│ Инвентаризация → Расхождение → ?       │
│ Нет автоматических действий            │
└─────────────────────────────────────────┘

НОВАЯ ПРОВЕРКА:
┌─────────────────────────────────────────┐
│ Инвентаризация → Расхождение           │
│       │                                 │
│       ▼                                 │
│ Проверить пороги:                       │
│   • > 5% → warning + уведомление       │
│   • > 10% → critical + инцидент        │
│   • > 20% → создать задачу проверки    │
└─────────────────────────────────────────┘
```

**Интеграция в InventoryService:**
```typescript
async processInventoryCount(data) {
  const difference = this.calculateDifference(data);

  // + Проверка порогов (если настроены)
  const threshold = await this.thresholdsService.findApplicable(data);
  if (threshold && difference > threshold.value) {
    await this.handleThresholdBreach(threshold, difference, data);
  }
}
```

---

### 10. CreateRouteStopsTable

**Влияние:** Детализация маршрутов

```
ТЕКУЩИЙ ROUTES WORKFLOW:
┌──────────────┐    ┌──────────────┐
│   Маршрут    │───▶│   Машины     │
│   (список)   │    │   (массив)   │
└──────────────┘    └──────────────┘
        │
        │ Нет детальной информации
        │ о каждой остановке
        ▼

НОВЫЙ ROUTES WORKFLOW:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Маршрут    │───▶│   Остановки  │───▶│   Статус     │
│              │    │   (детали)   │    │   каждой     │
└──────────────┘    └──────────────┘    └──────────────┘
                           │
                           ├── Плановое время
                           ├── Фактическое время
                           ├── Координаты
                           └── Связанные задачи
```

**Изменения в RoutesService:**
```typescript
// Расширить существующий метод
async createRoute(dto: CreateRouteDto) {
  const route = await this.routesRepo.save(dto);

  // + Создать остановки
  if (dto.stops?.length) {
    await this.routeStopsService.createStops(route.id, dto.stops);
  }

  return route;
}
```

---

### 11. CreateReconciliationMismatchesTable

**Влияние:** Детализация сверки

```
ТЕКУЩАЯ СВЕРКА:
┌─────────────────────────────────────────┐
│ ReconciliationRun → summary (JSON)     │
│ Все расхождения в одном поле           │
└─────────────────────────────────────────┘

НОВАЯ СВЕРКА:
┌─────────────────────────────────────────┐
│ ReconciliationRun                       │
│       │                                 │
│       ▼                                 │
│ ReconciliationMismatches (детали)      │
│   • Каждое расхождение отдельно        │
│   • Статус разрешения                  │
│   • Кто разрешил                       │
└─────────────────────────────────────────┘
```

**Изменения в ReconciliationService:**
```typescript
async processReconciliation(runId) {
  // Существующая логика...

  // + Сохранить детали расхождений
  for (const mismatch of mismatches) {
    await this.mismatchesRepo.save({
      run_id: runId,
      ...mismatch
    });
  }
}
```

---

## 🟠 ЗНАЧИТЕЛЬНОЕ ВЛИЯНИЕ НА WORKFLOW

### 12. AddTaskRejectionFields

**Влияние:** ИЗМЕНЕНИЕ Task Workflow

```
ТЕКУЩИЙ TASK WORKFLOW:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ pending  │───▶│in_progress│───▶│ completed│
└──────────┘    └──────────┘    └──────────┘
                                      │
                                      │ Нет отклонения
                                      ▼

НОВЫЙ TASK WORKFLOW:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ pending  │───▶│in_progress│───▶│ completed│
└──────────┘    └──────────┘    └──────────┘
      │               │
      │               │ НОВОЕ: отклонение
      │               ▼
      │         ┌──────────┐
      │         │ rejected │◀─── с указанием причины
      │         └──────────┘
      │               │
      │               │ Переназначение
      │               ▼
      └──────────────────────────────────────
```

**Необходимые изменения:**

1. **Backend - TasksService:**
```typescript
// Новый метод
async rejectTask(taskId: string, dto: RejectTaskDto) {
  const task = await this.findOne(taskId);

  task.status = 'rejected';
  task.rejected_by_user_id = dto.userId;
  task.rejected_at = new Date();
  task.rejection_reason = dto.reason;

  await this.tasksRepo.save(task);

  // Уведомить менеджера
  await this.notificationsService.notifyTaskRejected(task);

  return task;
}
```

2. **Backend - TasksController:**
```typescript
@Post(':id/reject')
@Roles('operator', 'manager')
async rejectTask(
  @Param('id') id: string,
  @Body() dto: RejectTaskDto,
  @CurrentUser() user: User,
) {
  return this.tasksService.rejectTask(id, { ...dto, userId: user.id });
}
```

3. **Frontend - Task Form:**
```tsx
// Добавить кнопку "Отклонить"
<Button onClick={() => setShowRejectModal(true)}>
  Отклонить задачу
</Button>

<RejectTaskModal
  open={showRejectModal}
  onConfirm={(reason) => rejectTask(taskId, reason)}
/>
```

4. **Mobile - Task Screen:**
```tsx
// Добавить опцию отклонения
<TaskActions>
  <Button onPress={startTask}>Начать</Button>
  <Button onPress={showRejectDialog}>Отклонить</Button>
</TaskActions>
```

5. **Bot - Commands:**
```typescript
// Новая команда
bot.action(/reject_task_(.+)/, async (ctx) => {
  const taskId = ctx.match[1];
  await ctx.reply('Укажите причину отклонения:');
  // ... обработка
});
```

**Риск:** 🟠 СРЕДНИЙ
- Требует изменения во всех клиентах
- Нужно обновить бизнес-логику
- Тестирование всех сценариев

---

### 13. EnhanceAlertsSystem

**Влияние:** ИЗМЕНЕНИЕ Alert Workflow

```
ТЕКУЩИЙ ALERT WORKFLOW:
┌──────────────┐    ┌──────────────┐
│  Триггер     │───▶│ Уведомление  │
│  алерта      │    │  (сразу)     │
└──────────────┘    └──────────────┘
        │
        │ Нет cooldown
        │ Спам уведомлениями
        ▼

НОВЫЙ ALERT WORKFLOW:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  Триггер     │───▶│  Cooldown    │───▶│ Уведомление  │
│  алерта      │    │  проверка    │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
                           │                    │
                           │                    ▼
                           │            ┌──────────────┐
                           │            │ Alert History│
                           │            └──────────────┘
                           │                    │
                           │                    ▼
                           │            ┌──────────────┐
                    Подождать           │ Эскалация    │
                    cooldown            │ (если не ACK)│
                                        └──────────────┘
```

**Необходимые изменения:**

1. **AlertsService:**
```typescript
async triggerAlert(rule: AlertRule, context: any) {
  // + Проверка cooldown
  if (rule.cooldown_minutes) {
    const lastTriggered = rule.last_triggered_at;
    const cooldownEnd = addMinutes(lastTriggered, rule.cooldown_minutes);
    if (new Date() < cooldownEnd) {
      return; // Пропустить - в cooldown
    }
  }

  // Создать запись в истории
  const alertHistory = await this.alertHistoryRepo.save({
    alert_rule_id: rule.id,
    status: 'triggered',
    triggered_at: new Date(),
    ...context
  });

  // Обновить rule
  await this.alertRulesRepo.update(rule.id, {
    last_triggered_at: new Date(),
    trigger_count: () => 'trigger_count + 1'
  });

  // Отправить уведомления
  await this.sendNotifications(rule, alertHistory);

  // + Запланировать эскалацию
  if (rule.escalation_minutes) {
    await this.scheduleEscalation(alertHistory, rule.escalation_minutes);
  }
}
```

2. **Новые endpoints:**
```typescript
@Post('alerts/:id/acknowledge')
async acknowledgeAlert(@Param('id') id: string, @Body() dto: AckDto) { }

@Post('alerts/:id/resolve')
async resolveAlert(@Param('id') id: string, @Body() dto: ResolveDto) { }

@Get('alerts/history')
async getAlertHistory(@Query() filters: AlertHistoryFilters) { }
```

3. **Frontend - Alerts Page:**
```tsx
// Новый UI для истории алертов
<AlertsHistoryTable
  data={alertHistory}
  onAcknowledge={handleAck}
  onResolve={handleResolve}
/>
```

---

## 🔴 ВЫСОКОЕ ВЛИЯНИЕ НА WORKFLOW

### 14. CreateIntelligentImportSystem

**Влияние:** НОВЫЙ Import Workflow (изолированный)

```
ТЕКУЩИЙ IMPORT:
┌──────────────┐    ┌──────────────┐
│ Загрузить    │───▶│ Импортировать│
│ файл         │    │ (сразу)      │
└──────────────┘    └──────────────┘
        │
        │ Нет валидации
        │ Нет preview
        ▼

НОВЫЙ INTELLIGENT IMPORT:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 1. Upload    │───▶│ 2. Classify  │───▶│ 3. Validate  │
│    файла     │    │    тип       │    │    данные    │
└──────────────┘    └──────────────┘    └──────────────┘
                                               │
┌──────────────┐    ┌──────────────┐           │
│ 6. Execute   │◀───│ 5. Approve   │◀──────────┘
│    импорт    │    │    план      │
└──────────────┘    └──────────────┘
        │                  ▲
        │                  │
        ▼           ┌──────────────┐
┌──────────────┐    │ 4. Preview   │
│ 7. Audit Log │    │    изменений │
└──────────────┘    └──────────────┘
```

**Это НОВЫЙ модуль, не затрагивает существующий импорт!**

```typescript
// Новый модуль - полностью изолирован
@Module({
  imports: [TypeOrmModule.forFeature([
    ImportSession,
    ImportTemplate,
    ImportAuditLog,
    SchemaDefinition,
    ValidationRule
  ])],
  controllers: [IntelligentImportController],
  providers: [
    IntelligentImportService,
    FileClassifierService,
    ValidationService,
    ImportExecutorService
  ]
})
export class IntelligentImportModule {}
```

**Существующий импорт:** Продолжает работать без изменений
**Новый импорт:** Доступен по отдельному URL `/admin/intelligent-import`

---

### 15. CreateEquipmentManagementTables

**Влияние:** РАСШИРЕНИЕ Equipment/Maintenance Workflow

```
ТЕКУЩИЙ MAINTENANCE WORKFLOW:
┌──────────────┐    ┌──────────────┐
│ Создать      │───▶│ Выполнить    │
│ задачу       │    │ ремонт       │
│ maintenance  │    │              │
└──────────────┘    └──────────────┘
        │
        │ Нет учёта компонентов
        │ Нет запчастей
        ▼

НОВЫЙ MAINTENANCE WORKFLOW:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 1. Диагностика│───▶│ 2. Компонент │───▶│ 3. Запчасти  │
│    машины    │    │    требует   │    │    со склада │
│              │    │    замены    │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
                                               │
┌──────────────┐    ┌──────────────┐           │
│ 6. История   │◀───│ 5. Задача    │◀──────────┘
│ обслуживания │    │    завершена │
└──────────────┘    └──────────────┘
        │                  │
        │                  │
        ▼                  ▼
┌──────────────┐    ┌──────────────┐
│ 7. Следующее │    │ 8. Перемещение│
│ ТО по графику│    │    компонента │
└──────────────┘    └──────────────┘
```

**⚠️ ВАЖНО: Проверить существующий equipment модуль в VendHub OS**

```typescript
// Перед реализацией:
// 1. Найти существующие entities equipment
// 2. Определить пересечения
// 3. Решить: расширить или заменить
```

---

## 📋 ЧЕКЛИСТ БЕЗОПАСНОЙ ИНТЕГРАЦИИ

### Для каждой миграции с влиянием на workflow:

```
□ 1. Feature flag создан и выключен по умолчанию
□ 2. Новый код не ломает существующий
□ 3. API backwards compatible
□ 4. Миграция reversible (есть down())
□ 5. Тесты на новую функциональность
□ 6. Тесты на регрессию старой
□ 7. Документация обновлена
□ 8. Frontend готов к новым полям (nullable handling)
□ 9. Mobile готов к новым полям
□ 10. Bot обработает новые статусы
```

---

## 🎯 РЕКОМЕНДУЕМАЯ СТРАТЕГИЯ

### Фаза 1: Изолированные модули (без влияния)
```
Week 1-2:
├── CreateDictionariesTables ✅
├── CreateDashboardWidgetsTable ✅
├── CreateDailyStatsTable ✅
├── CreateOperatorRatingsTable ✅
└── AddPerformanceIndexes ✅

Результат: Новая функциональность, старая не затронута
```

### Фаза 2: Расширения с feature flags
```
Week 3-4:
├── CreateInventoryReservationsTable (flag: off)
├── CreateInventoryDifferenceThresholdsTable (flag: off)
├── CreateRouteStopsTable (flag: off)
└── CreateReconciliationMismatchesTable (flag: off)

Результат: Код готов, включается по желанию
```

### Фаза 3: Изменения workflow (с тестированием)
```
Week 5-6:
├── AddTaskRejectionFields
│   ├── Backend: новый endpoint + логика
│   ├── Frontend: UI + modal
│   ├── Mobile: action button
│   └── Testing: все сценарии
│
└── EnhanceAlertsSystem
    ├── Backend: cooldown + history + escalation
    ├── Frontend: history table + actions
    └── Testing: timing scenarios

Результат: Улучшенные workflow с полным тестированием
```

### Фаза 4: Комплексные системы (отдельные спринты)
```
Week 7-10:
├── CreateIntelligentImportSystem (отдельный модуль)
└── CreateEquipmentManagementTables (после аудита)

Результат: Новые major features
```

---

## 📊 ИТОГОВАЯ МАТРИЦА БЕЗОПАСНОСТИ

| Миграция | Влияние на workflow | Изменения в коде | Можно сразу? |
|----------|---------------------|------------------|--------------|
| Dictionaries | ❌ Нет | ➕ Новый модуль | ✅ ДА |
| DashboardWidgets | ❌ Нет | ➕ Новый модуль | ✅ ДА |
| DailyStats | ❌ Нет | ➕ CRON job | ✅ ДА |
| AnalyticsSnapshots | ❌ Нет | ➕ CRON job | ✅ ДА |
| CustomReports | ❌ Нет | ➕ Новый модуль | ✅ ДА |
| OperatorRatings | ❌ Нет | ➕ CRON + UI | ✅ ДА |
| RecipeSnapshots | ❌ Нет | 🔄 Хук в update | ✅ ДА |
| PerformanceIndexes | ❌ Нет | ❌ Только DDL | ✅ ДА |
| InventoryReservations | 🔄 Опционально | 🔄 TasksService | ⚠️ С flag |
| InventoryThresholds | 🔄 Опционально | 🔄 InventoryService | ⚠️ С flag |
| RouteStops | 🔄 Опционально | 🔄 RoutesService | ⚠️ С flag |
| ReconciliationMismatches | 🔄 Опционально | 🔄 ReconciliationService | ⚠️ С flag |
| **TaskRejection** | 🔄 **Меняет workflow** | 🔄 Все клиенты | ⚠️ С тестами |
| **AlertsEnhance** | 🔄 **Меняет workflow** | 🔄 AlertsService | ⚠️ С тестами |
| IntelligentImport | ➕ Новый workflow | ➕ Новый модуль | ✅ Изолирован |
| EquipmentManagement | 🔄 **Меняет workflow** | 🔄 Аудит нужен | ❌ После проверки |

---

**Вывод:** 8 из 16 миграций можно делать сразу без риска. 4 миграции — с feature flags. 4 миграции требуют детального планирования.
