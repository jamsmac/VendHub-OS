# ПРОМТ: Создание финальной структуры интерфейса VendHub OS

**Версия:** 1.1
**Дата:** 26 января 2026
**Источники:** 10 документов (8 спецификаций + 2 DOCX системной документации)
**Цель:** Генерация окончательной детальной структуры UI — что где расположено и для чего

---

## 🎯 ЗАДАЧА

Создай **финальную унифицированную структуру интерфейса** платформы VendHub OS, которая:

1. Объединяет навигацию из всех приложенных документов в единую согласованную иерархию
2. Интегрирует модуль Master Data Management (справочники) во все соответствующие разделы
3. Описывает каждый экран: путь, назначение, основные элементы, связи
4. Учитывает 4 интерфейса: Admin Dashboard, Staff App, Client App, Investor Portal

---

## 📁 ИСХОДНЫЕ ДОКУМЕНТЫ

Для анализа используй следующие документы:

| # | Документ | Что извлечь |
|---|----------|-------------|
| 1 | `UI_UX_SPECIFICATION.md` | Полная структура навигации Admin Dashboard, карточки товаров, AI Import |
| 2 | `VHM24_Master_Data_Specification_v1.0.md` | Directory Builder Wizard (6 шагов), типы справочников, Entry Cards |
| 3 | `VHM24_Master_Data_Appendix_F_React_Frontend.md` | React компоненты: DirectorySelect, EntryList, ImportWizard |
| 4 | `VHM24_Master_Data_Appendix_C_Diagrams.md` | Mermaid диаграммы flows |
| 5 | `VENDHUB_UI_DOCUMENTATION_COMPLETE.md` | Текущая документация 20 экранов Web Admin |

---

## 📐 ТРЕБУЕМАЯ СТРУКТУРА ДОКУМЕНТА

### РАЗДЕЛ 1: ОБЗОР ПЛАТФОРМЫ

```
1.1 Четыре интерфейса платформы
    - Admin Dashboard (Web) — Owner, Admin, Manager
    - Staff App (Mobile + PWA) — Operator, Technician, Warehouse
    - Client App (Mobile + TWA) — Покупатели
    - Investor Portal (Web) — Инвесторы, Франчайзи

1.2 Технологический стек
    - Frontend: Next.js 16, React Native, Vite + React
    - Backend: NestJS 11, PostgreSQL 16
    - UI: shadcn/ui, Radix UI, Tailwind CSS

1.3 Система ролей (RBAC)
    Иерархия из 7 уровней с указанием доступа к разделам
```

### РАЗДЕЛ 2: ADMIN DASHBOARD — ПОЛНАЯ СТРУКТУРА

```
📊 ОБЗОР
├── /dashboard — Dashboard
│   ├── Назначение: KPI виджеты, графики, быстрые действия
│   ├── Элементы: StatCards, Charts (Recharts), RecentActivity, Alerts
│   └── Роли: Все
│
├── /dashboard/analytics — Аналитика
│   ├── Назначение: Продажи, тренды, сравнения, AI-инсайты
│   ├── Элементы: DateRangePicker, Charts, Filters, Export
│   └── Роли: Manager+
│
├── /dashboard/map — Карта
│   ├── Назначение: Real-time карта автоматов с состояниями
│   ├── Элементы: GoogleMap, MachineMarkers, StatusFilters, ClusterView
│   └── Роли: Все
│
└── /dashboard/monitoring — Мониторинг
    ├── Назначение: Состояние системы, алерты, телеметрия
    ├── Элементы: AlertsTable, StatusBadges, MetricsCharts
    └── Роли: Admin+

🏭 АВТОМАТЫ
├── /dashboard/machines — Список автоматов
│   ├── Назначение: Все машины с фильтрами и поиском
│   ├── Элементы: DataTable, MachineCard, StatusFilters, BulkActions
│   ├── Подразделы:
│   │   ├── ?type=coffee — Кофейные автоматы
│   │   └── ?type=snack — Снековые автоматы
│   └── Роли: Все
│
├── /dashboard/machines/[id] — Карточка автомата
│   ├── Назначение: Детальная информация, статистика, действия
│   ├── Элементы: MachineHeader, Tabs (Info, Slots, Telemetry, History)
│   └── Роли: Operator+
│
├── /dashboard/equipment — Оборудование (ОС)
│   ├── Назначение: Учёт основных средств (упрощённый)
│   ├── Элементы: EquipmentTable, EquipmentCard, MovementHistory
│   └── Роли: Manager+
│
├── /dashboard/spare-parts — Запчасти
│   ├── Назначение: Справочник запчастей ★ СПРАВОЧНИК (MANUAL)
│   ├── Элементы: DirectoryEntryList, EntryCard, InlineCreate
│   ├── MDM: directory_slug = "spare_parts"
│   └── Роли: Technician+
│
├── /dashboard/planograms — Слоты и планограммы
│   ├── Назначение: Расположение товаров в машине
│   ├── Элементы: SlotGrid, DragDropPlanogram, ProductSelector
│   └── Роли: Manager+
│
├── /dashboard/bunkers — Бункеры и миксеры
│   ├── Назначение: Конфигурация кофейных машин
│   ├── Элементы: BunkerConfig, IngredientAssignment
│   └── Роли: Technician+
│
└── /dashboard/maintenance — Техобслуживание
    ├── Назначение: Графики ТО, история ремонтов
    ├── Элементы: MaintenanceCalendar, ServiceHistory, WorkTypeSelector
    ├── Связь: work_types — СПРАВОЧНИК (MANUAL)
    └── Роли: Technician+

📦 ТОВАРЫ И НОМЕНКЛАТУРА
├── /dashboard/products — Каталог товаров
│   ├── Назначение: Все товары ★ СПРАВОЧНИК (MANUAL)
│   ├── Элементы: DirectoryEntryList с ProductCard
│   ├── MDM: directory_slug = "goods"
│   ├── Подтипы:
│   │   ├── 🍵 Напитки — с рецептурой, себестоимость = Σ ингредиентов
│   │   └── 🍫 Снеки — перепродажа, закупка + наценка
│   └── Роли: Все (create: Manager+)
│
├── /dashboard/products/[id] — Карточка товара
│   ├── Назначение: Детали товара, рецептура, статистика
│   ├── Элементы: ProductHeader, Tabs (Info, Recipe, Pricing, Stats)
│   ├── Для напитков: RecipeBuilder, CostCalculator
│   ├── Для снеков: PurchasePriceInput, MarkupCalculator
│   └── Роли: Manager+ (edit)
│
├── /dashboard/ingredients — Ингредиенты
│   ├── Назначение: Сырьё для напитков ★ СПРАВОЧНИК (MANUAL)
│   ├── Элементы: DirectoryEntryList, IngredientCard
│   ├── MDM: directory_slug = "ingredients"
│   └── Роли: Manager+
│
├── /dashboard/recipes — Рецептуры
│   ├── Назначение: Состав напитков
│   ├── Элементы: RecipeList, RecipeBuilder, IngredientSelector
│   ├── Связь: ingredients, goods
│   └── Роли: Manager+
│
├── /dashboard/pricing — Ценообразование
│   ├── Назначение: Управление ценами и наценками
│   ├── Элементы: PriceMatrix, BulkPriceEditor, MarginCalculator
│   └── Роли: Accountant+
│
└── /dashboard/import-export — Импорт/Экспорт
    ├── Назначение: Excel, CSV операции
    ├── Элементы: ImportWizard (AI), ExportBuilder
    └── Роли: Manager+

📚 СПРАВОЧНИКИ (Master Data Management)
├── /dashboard/directories — Все справочники
│   ├── Назначение: Список всех справочников системы
│   ├── Элементы: DirectoryList, DirectoryCard, CreateButton
│   └── Роли: Admin+ (manage), Все (view)
│
├── /dashboard/directories/create — Создать справочник
│   ├── Назначение: Directory Builder Wizard (6 шагов)
│   ├── Шаги:
│   │   ├── Step 1: Выбор типа (MANUAL/EXTERNAL/PARAM/TEMPLATE)
│   │   ├── Step 2: Базовая информация (name, slug, scope)
│   │   ├── Step 3: Конструктор полей (Field Builder + Live Preview)
│   │   ├── Step 4: Источник данных (только для EXTERNAL)
│   │   ├── Step 5: Настройки (inline_create, offline, prefetch)
│   │   └── Step 6: Права доступа (RBAC matrix)
│   └── Роли: Admin+
│
├── /dashboard/directories/[slug] — Записи справочника
│   ├── Назначение: Просмотр и управление записями
│   ├── Элементы:
│   │   ├── EntryList (таблица) — для обычных
│   │   └── EntryTree (дерево) — для is_hierarchical=true
│   ├── Функции: Search, Filter, BulkActions, InlineCreate
│   └── Роли: По настройкам справочника
│
├── /dashboard/directories/[slug]/[id] — Карточка записи
│   ├── Назначение: Детали записи справочника
│   ├── Элементы: EntryCard, FieldsView, AuditHistory, RelatedLinks
│   ├── OFFICIAL: Read-only с badge 🛡️
│   ├── LOCAL: Editable с badge ✍️
│   └── Роли: По настройкам справочника
│
├── /dashboard/directories/[slug]/import — Импорт записей
│   ├── Назначение: AI-powered Import Wizard
│   ├── Шаги:
│   │   ├── Upload: File dropzone (xlsx, csv, json)
│   │   ├── Mapping: Column auto-detection + manual adjustment
│   │   ├── Preview: First 10 rows + validation errors
│   │   └── Process: Progress bar + results
│   ├── Режимы: CREATE_ONLY, UPSERT, UPDATE_ONLY, DRY_RUN
│   └── Роли: Manager+ (if can_bulk_import)
│
├── /dashboard/categories — Категории товаров ★ PARAM
│   ├── MDM: directory_slug = "categories"
│   └── Особенность: is_hierarchical = true (дерево)
│
├── /dashboard/ikpu — Коды ИКПУ ★ EXTERNAL
│   ├── MDM: directory_slug = "ikpu"
│   ├── Источник: Государственный классификатор
│   └── Особенность: OFFICIAL (read-only) + LOCAL overlay
│
├── /dashboard/packaging — Типы упаковки ★ PARAM
│   └── MDM: directory_slug = "packaging"
│
├── /dashboard/barcodes — Штрих-коды
│   └── Связь: goods.data.barcode
│
├── /dashboard/vat-rates — Ставки НДС ★ PARAM
│   └── MDM: directory_slug = "vat_rates"
│
├── /dashboard/marking — Маркировка (Честный знак)
│   └── Интеграция: API Честного знака
│
├── /dashboard/units — Единицы измерения ★ PARAM (system)
│   └── MDM: directory_slug = "units"
│
├── /dashboard/suppliers — Поставщики ★ MANUAL
│   ├── MDM: directory_slug = "suppliers"
│   └── Связь: contractors (тип = supplier)
│
└── /dashboard/locations — Локации ★ MANUAL
    ├── MDM: directory_slug = "locations"
    └── Поля: name, address, geo, region, type

📦 СКЛАД И ЛОГИСТИКА
├── /dashboard/inventory — Остатки
│   ├── Назначение: 3-уровневый инвентарь
│   ├── Уровни:
│   │   ├── Склад — warehouse_stock
│   │   ├── Оператор — operator_stock
│   │   └── Машина — machine_stock
│   └── Роли: Warehouse+
│
├── /dashboard/receiving — Приёмка товара
│   ├── Назначение: От поставщиков на склад
│   └── Элементы: ReceivingForm, DocumentUpload, ProductScanner
│
├── /dashboard/distribution — Выдача операторам
│   ├── Назначение: Перемещение со склада на оператора
│   └── Элементы: DistributionForm, OperatorSelect, ProductList
│
├── /dashboard/stocktaking — Инвентаризация
│   ├── Назначение: Сверка фактических и учётных остатков
│   └── Элементы: StocktakingWizard, DiscrepancyReport
│
├── /dashboard/forecasting — Прогнозирование
│   ├── Назначение: AI-предсказание когда закончится товар
│   └── Элементы: ForecastChart, AlertSettings, AutoOrderTrigger
│
└── /dashboard/auto-orders — Автозаказы
    ├── Назначение: Автоматические заявки поставщикам
    └── Элементы: AutoOrderRules, SupplierMapping, OrderHistory

✅ ОПЕРАЦИИ
├── /dashboard/tasks — Задачи
│   ├── Назначение: Kanban-доска задач
│   ├── Элементы: KanbanBoard, TaskCard, CreateTaskModal, Filters
│   ├── Колонки: TODO, IN_PROGRESS, REVIEW, DONE
│   └── Роли: Operator+ (view), Manager+ (create/assign)
│
├── /dashboard/tasks/[id] — Карточка задачи
│   ├── Элементы: TaskHeader, Checklist, PhotoReports, Comments, History
│   └── Роли: Assignee, Manager+
│
├── /dashboard/schedule — Расписание
│   ├── Назначение: Планировщик задач, календарь
│   └── Элементы: Calendar (Week/Month), TaskScheduler, RecurringRules
│
├── /dashboard/routes — Маршруты
│   ├── Назначение: Маршруты операторов с оптимизацией
│   └── Элементы: RouteMap, RouteBuilder, OptimizeButton
│
├── /dashboard/incidents — Инциденты
│   ├── Назначение: Поломки, проблемы с оборудованием
│   └── Элементы: IncidentList, IncidentForm, PriorityBadges
│
├── /dashboard/complaints — Жалобы клиентов
│   ├── Назначение: QR-жалобы от клиентов
│   └── Элементы: ComplaintsList, ComplaintDetail, ResponseForm
│
├── /dashboard/collection — Инкассация
│   ├── Назначение: Сбор наличных из автоматов
│   └── Элементы: CollectionSchedule, CollectionReport, CashCount
│
└── /dashboard/photo-reports — Фото-отчёты
    ├── Назначение: Before/After фото по задачам
    └── Элементы: PhotoGallery, CompareView, ApprovalButtons

💰 ФИНАНСЫ
├── /dashboard/transactions — Транзакции
│   ├── Назначение: Все платежи (online + cash)
│   └── Элементы: TransactionTable, Filters, ExportButton
│
├── /dashboard/reconciliation — Сверка данных
│   ├── Назначение: Reconciliation продаж vs платежей
│   └── Элементы: ReconciliationWizard, DiscrepancyList
│
├── /dashboard/commissions — Комиссии
│   ├── Назначение: Расчёт комиссий локации, партнёров
│   └── Элементы: CommissionRules, CalculationReport
│
├── /dashboard/contracts — Договоры
│   ├── Назначение: Документы с контрагентами
│   └── Элементы: ContractList, ContractUpload, ExpiryAlerts
│
├── /dashboard/contractors — Контрагенты ★ СПРАВОЧНИК
│   ├── MDM: directory_slug = "contractors"
│   └── Типы: supplier, landlord, partner, service_provider
│
├── /dashboard/cost-analysis — Себестоимость
│   ├── Назначение: Анализ маржинальности
│   └── Элементы: CostBreakdown, MarginChart, ProductRanking
│
└── /dashboard/reports — Отчёты
    ├── Назначение: PDF, Excel генерация
    └── Элементы: ReportBuilder, TemplateSelector, ScheduleExport

👥 КЛИЕНТЫ И ЛОЯЛЬНОСТЬ
├── /dashboard/customers — Клиенты
│   ├── Назначение: База клиентов (из Client App)
│   └── Элементы: CustomerTable, CustomerProfile, PurchaseHistory
│
├── /dashboard/orders — Заказы
│   ├── Назначение: История заказов клиентов
│   └── Элементы: OrderTable, OrderDetail, RefundButton
│
├── /dashboard/loyalty — Программа лояльности
│   ├── Назначение: Уровни, баллы, streaks
│   └── Элементы: LevelConfig, PointsRules, StreakSettings
│
├── /dashboard/quests — Квесты и достижения
│   ├── Назначение: Геймификация
│   └── Элементы: QuestBuilder, AchievementList, Rewards
│
├── /dashboard/promocodes — Промокоды
│   ├── Назначение: Акции и скидки
│   └── Элементы: PromocodeTable, CreatePromoModal, UsageStats
│
├── /dashboard/bonuses — Бонусы
│   ├── Назначение: Бонусы по товарам/автоматам
│   └── Элементы: BonusRules, ProductBonusMapping
│
└── /dashboard/recommendations — Рекомендации
    ├── Назначение: AI-рекомендации для клиентов
    └── Элементы: RecommendationEngine, A/BTestResults

💳 ИНТЕГРАЦИИ
├── /dashboard/integrations — Обзор интеграций
│   └── Элементы: IntegrationCards, StatusIndicators
│
├── /dashboard/integrations/payments — Платёжные системы
│   ├── Системы: Payme, Click, Uzum, TG Stars, Наличные
│   └── Элементы: PaymentSystemCard, CredentialsForm, WebhookConfig
│
├── /dashboard/integrations/fiscal — Фискализация
│   ├── Системы: MultiKassa, Честный знак
│   └── Элементы: FiscalConfig, ReceiptLog, ErrorAlerts
│
├── /dashboard/integrations/external — Внешние API
│   ├── Назначение: Конструктор интеграций
│   └── Элементы: APIBuilder, EndpointConfig, MappingEditor
│
├── /dashboard/integrations/vending — Вендинговые протоколы
│   ├── Протоколы: MDB, DEX, Telemetry
│   └── Элементы: ProtocolConfig, DataParser
│
└── /dashboard/integrations/crm — CRM/ERP
    ├── Системы: 1C, Битрикс24
    └── Элементы: SyncConfig, MappingRules

🤖 AI И АВТОМАТИЗАЦИЯ
├── /dashboard/ai/assistant — AI Ассистент
│   ├── Назначение: Чат с ИИ для помощи
│   └── Элементы: ChatInterface, ContextPanel, SuggestedActions
│
├── /dashboard/ai/agents — AI Агенты
│   ├── Назначение: Автономные агенты
│   ├── Подразделы:
│   │   ├── list — Список настроенных агентов
│   │   ├── running — Текущие выполняемые задачи
│   │   ├── logs — История и результаты
│   │   └── create — Конструктор агентов
│   └── Элементы: AgentCard, TaskQueue, LogViewer
│
├── /dashboard/ai/prompts — Промпты и скиллы
│   ├── Назначение: Библиотека промптов
│   └── Элементы: PromptLibrary, PromptEditor, VersionHistory
│
├── /dashboard/ai/providers — AI Провайдеры
│   ├── Провайдеры: OpenAI, Claude, Gemini, Local LLM
│   └── Элементы: ProviderConfig, UsageStats, LimitSettings
│
├── /dashboard/ai/automations — Автоматизации
│   ├── Назначение: Триггеры и цепочки действий
│   └── Элементы: AutomationBuilder, TriggerConfig, ActionChain
│
├── /dashboard/ai/import — AI Импорт
│   ├── Назначение: Умный импорт файлов
│   ├── Агенты:
│   │   ├── Document Classification Agent
│   │   ├── Schema Mapping Agent
│   │   ├── Data Extraction Agent
│   │   ├── Validation Agent
│   │   ├── Reference Resolution Agent
│   │   ├── Reconciliation Agent
│   │   └── Learning Agent
│   └── Элементы: FileDropzone, MappingPreview, FeedbackLoop
│
└── /dashboard/ai/analytics — AI Аналитика
    ├── Назначение: Предиктивная аналитика
    └── Элементы: PredictionCharts, AnomalyAlerts, Insights

🔔 КОММУНИКАЦИИ
├── /dashboard/notifications — Центр уведомлений
│   └── Элементы: NotificationList, ReadAllButton, Settings
│
├── /dashboard/telegram — Telegram бот
│   └── Элементы: BotConfig, CommandList, WebhookStatus
│
├── /dashboard/email — Email рассылки
│   └── Элементы: EmailTemplates, CampaignBuilder, Analytics
│
├── /dashboard/push — Push-уведомления
│   └── Элементы: VAPIDConfig, SubscriberList, SendPush
│
├── /dashboard/sms — SMS
│   ├── Провайдеры: Eskiz, PlayMobile
│   └── Элементы: SMSConfig, BalanceWidget, SendSMS
│
└── /dashboard/templates — Шаблоны сообщений
    └── Элементы: TemplateEditor, VariablesList, Preview

⚙️ АДМИНИСТРИРОВАНИЕ
├── /dashboard/users — Пользователи
│   ├── Назначение: Управление пользователями системы
│   └── Элементы: UserTable, UserForm, RoleAssignment
│
├── /dashboard/roles — Роли и права
│   ├── Назначение: RBAC настройка
│   └── Элементы: RoleMatrix, PermissionEditor
│
├── /dashboard/organizations — Организации
│   ├── Назначение: Multi-tenant управление
│   └── Элементы: OrgTree, OrgSettings, UserAssignment
│
├── /dashboard/audit — Аудит
│   ├── Назначение: Логи всех действий
│   └── Элементы: AuditLog, Filters, ExportButton
│
├── /dashboard/security — Безопасность
│   ├── Назначение: 2FA, сессии, пароли
│   └── Элементы: SecuritySettings, SessionList, 2FAConfig
│
├── /dashboard/backups — Резервные копии
│   └── Элементы: BackupSchedule, RestoreButton, History
│
└── /dashboard/settings — Настройки
    └── Элементы: SettingsTabs (General, Appearance, Notifications)
```

### РАЗДЕЛ 3: STAFF APP — СТРУКТУРА

```
📱 BOTTOM TAB NAVIGATION

┌──────────────────────────────────────────────────────────┐
│                                                          │
│  [Tasks]    [Map]    [Inventory]    [Profile]           │
│    📋        🗺️         📦           👤                  │
│                                                          │
└──────────────────────────────────────────────────────────┘

TASKS TAB
├── /tasks — Список задач
│   ├── Назначение: Задачи оператора на сегодня
│   ├── Элементы: TaskList, SwipeActions, StatusFilter
│   └── Offline: ✅ Синхронизируется
│
├── /tasks/[id] — Детали задачи
│   ├── Элементы: TaskInfo, Checklist, PhotoCapture, Complete
│   └── Offline: ✅ Кэшируется
│
└── /tasks/create — Создать задачу
    ├── Элементы: QuickTaskForm, MachineSelector
    └── Роли: Manager+

MAP TAB
├── /map — Карта автоматов
│   ├── Назначение: Автоматы на маршруте
│   ├── Элементы: MapView, MachineMarkers, NavigateButton
│   └── Offline: ✅ Офлайн карты (опционально)
│
└── /map/machine/[id] — Быстрый просмотр
    └── Элементы: MachineQuickView, StatusIndicator

INVENTORY TAB
├── /inventory — Мой инвентарь
│   ├── Назначение: Товары у оператора
│   ├── Элементы: ProductList, StockCount, TransferButton
│   └── Offline: ✅ Синхронизируется
│
├── /inventory/load — Загрузка в машину
│   ├── Элементы: LoadingForm, BarcodeScanner, Confirm
│   └── Offline: ✅ Queue на отправку
│
└── /inventory/transfer — Передача товара
    └── Элементы: TransferForm, OperatorSelect

PROFILE TAB
├── /profile — Профиль
│   └── Элементы: Avatar, Stats, Settings, Logout
│
├── /profile/work-log — Табель
│   └── Элементы: WorkCalendar, HoursCount, CheckIn/Out
│
└── /profile/settings — Настройки
    └── Элементы: NotificationSettings, ThemeToggle, Language
```

### РАЗДЕЛ 4: CLIENT APP — СТРУКТУРА

```
📱 TAB NAVIGATION

┌──────────────────────────────────────────────────────────┐
│                                                          │
│  [Home]    [Catalog]    [Orders]    [Profile]           │
│    🏠        📦          📋          👤                   │
│                                                          │
└──────────────────────────────────────────────────────────┘

HOME TAB
├── /home — Главная
│   ├── Элементы: WelcomeBanner, NearbyMachines, Recommendations
│   └── Назначение: Персонализированный feed

CATALOG TAB
├── /catalog — Каталог
│   ├── Элементы: CategoryTabs, ProductGrid, SearchBar
│   └── Назначение: Просмотр товаров
│
├── /catalog/[productId] — Товар
│   └── Элементы: ProductCard, NutritionInfo, AddToCart
│
└── /catalog/machine/[machineId] — Автомат
    └── Элементы: MachineInfo, AvailableProducts, Map

ORDERS TAB
├── /orders — История заказов
│   └── Элементы: OrderList, StatusBadge, Reorder
│
└── /orders/[id] — Детали заказа
    └── Элементы: OrderDetail, Receipt, Support

PROFILE TAB
├── /profile — Профиль
│   └── Элементы: Avatar, LoyaltyCard, Stats
│
├── /profile/loyalty — Программа лояльности
│   └── Элементы: LevelProgress, Points, Rewards
│
├── /profile/quests — Квесты
│   └── Элементы: ActiveQuests, Achievements, Rewards
│
└── /profile/settings — Настройки
    └── Элементы: Notifications, Language, DeleteAccount
```

### РАЗДЕЛ 5: INVESTOR PORTAL — СТРУКТУРА

```
WEB PORTAL

📊 DASHBOARD
├── /investor — Dashboard
│   ├── Элементы: ROICard, RevenueChart, PortfolioValue
│   └── Назначение: Обзор инвестиций

📈 PORTFOLIO
├── /investor/portfolio — Портфель машин
│   └── Элементы: MachineList, PerformanceChart
│
└── /investor/portfolio/[id] — Детали машины
    └── Элементы: MachineStats, RevenueBreakdown

💰 FINANCIALS
├── /investor/revenue — Доходы
│   └── Элементы: RevenueTable, PeriodSelector, Export
│
├── /investor/dividends — Дивиденды
│   └── Элементы: DividendHistory, PayoutSchedule
│
└── /investor/documents — Документы
    └── Элементы: ContractList, ReportDownload

⚙️ SETTINGS
└── /investor/settings — Настройки
    └── Элементы: NotificationPrefs, PaymentDetails
```

### РАЗДЕЛ 6: ОБЩИЕ UI КОМПОНЕНТЫ (Master Data)

```
📦 DIRECTORY SELECT COMPONENT

Назначение: Универсальный компонент выбора из справочника

Использование:
- Выбор категории товара
- Выбор поставщика
- Выбор локации
- Выбор единицы измерения
- Выбор кода ИКПУ
- И др.

Анатомия:
┌─────────────────────────────────────────────────────────────┐
│ 🔍 Поиск...                                     [Spinner]   │
├─────────────────────────────────────────────────────────────┤
│ НЕДАВНИЕ                                                    │
│   🛡️ [name] ([code])                             [2m ago]   │
│   ✍️ [name] ([code])                             [1d ago]   │
├─────────────────────────────────────────────────────────────┤
│ РЕЗУЛЬТАТЫ                                                  │
│   🛡️ [OFFICIAL результаты]                                  │
│   ✍️ [LOCAL результаты]                                     │
├─────────────────────────────────────────────────────────────┤
│ ➕ Добавить "[query]" как новую запись                      │
└─────────────────────────────────────────────────────────────┘

Props:
- directorySlug: string — код справочника
- value: UUID | UUID[]
- onChange: (value) => void
- allowCreate: boolean — разрешить inline create
- multiple: boolean — множественный выбор

📋 ENTRY LIST COMPONENT

Назначение: Таблица записей справочника

Колонки:
- [ ] Checkbox
- Name (с badge OFFICIAL/LOCAL)
- Code
- [Dynamic fields where show_in_list=true]
- Actions (View, Edit, Archive)

Features:
- Sorting
- Filtering
- Bulk actions
- Pagination
- Search

🌳 ENTRY TREE COMPONENT

Назначение: Иерархический вид (для is_hierarchical=true)

Features:
- Expand/Collapse
- Lazy loading children
- Drag-and-drop reordering
- Breadcrumbs

📝 ENTRY CARD COMPONENT

Назначение: Карточка записи справочника

Sections:
- Header: Name, Code, Status, Origin badge
- Fields: Dynamic fields from directory_fields
- Relations: Links to related entries
- Audit: Created, Updated, History
- Actions: Edit, Archive (if LOCAL)

🔄 IMPORT WIZARD COMPONENT

Назначение: 4-шаговый импорт с AI

Steps:
1. Upload: File dropzone
2. Mapping: Auto-detected columns + manual fixes
3. Preview: 10 rows + validation
4. Process: Progress + results
```

### РАЗДЕЛ 7: DESIGN SYSTEM

```
🎨 ЦВЕТА "WARM BREW"

Primary (Espresso):     oklch(0.35 0.06 50)  — #5D4037
Accent (Caramel):       oklch(0.75 0.12 70)  — #D4A574
Background (Cream):     oklch(0.98 0.008 85) — #FDF8F3
Success (Mint):         oklch(0.7 0.1 160)   — #7CB69D
Error:                  oklch(0.6 0.2 25)    — #EF4444
Warning:                oklch(0.75 0.15 85)  — #F59E0B

📝 ТИПОГРАФИКА

Display: 'Playfair Display', Georgia, serif
Sans:    'DM Sans', system-ui, sans-serif
Mono:    'JetBrains Mono', monospace

🧩 UI БИБЛИОТЕКИ

- shadcn/ui — компоненты
- Radix UI — primitives
- Lucide React — иконки
- Recharts 2 — графики
- Framer Motion — анимации
- Google Maps API — карты
```

---

## ✅ ТРЕБОВАНИЯ К ВЫХОДНОМУ ДОКУМЕНТУ

1. **Формат:** Markdown с ASCII wireframes
2. **Язык:** Русский (технические термины на английском)
3. **Объём:** 80-120 страниц
4. **Структура:** Иерархическое дерево навигации с описанием каждого узла
5. **Для каждого экрана:**
   - Путь (route)
   - Назначение (1-2 предложения)
   - Основные элементы (компоненты)
   - Связь с MDM (если есть)
   - Роли доступа

---

## 🏷️ МЕТАДАННЫЕ

```yaml
document:
  title: "VendHub OS — Финальная структура интерфейса"
  version: "3.0.0"
  date: "2026-01-26"
  type: "UI Structure Specification"

scope:
  interfaces: 4
  admin_screens: 80+
  staff_screens: 15
  client_screens: 12
  investor_screens: 8

  mdm_directories: 15+
  api_endpoints: 222+
```

---

## 🌟 КЛЮЧЕВЫЕ ФИШКИ ИЗ ВСЕХ ДОКУМЕНТОВ (обязательно включить)

### Из VHM24_Master_Data_Specification:

1. **OFFICIAL vs LOCAL разделение** — визуальная маркировка происхождения данных
   - 🛡️ Щит = OFFICIAL (синий badge, read-only)
   - ✍️ Карандаш = LOCAL (серый badge, editable)
   - Группировка в dropdown по происхождению

2. **Directory Builder Wizard** — 6-шаговое создание справочников
   - Live Preview справа от конструктора полей
   - Drag-and-drop сортировка полей
   - Тестовый запрос для EXTERNAL источников

3. **Inline Create везде** — добавление записи прямо в поле выбора
   - "➕ Добавить [query] как новую запись"
   - Без перехода на другой экран
   - Instant появление в списке

4. **Deprecation flow** — для устаревших OFFICIAL записей
   - Показ replacement_entry
   - "Заменить везде" / "Оставить как есть"
   - Счётчик использований

5. **Recent selections** — недавние выборы пользователя
   - Показываются первыми в dropdown
   - С временной меткой "2m ago", "1d ago"
   - Персональные для каждого user

6. **Hierarchical directories** — древовидные справочники
   - EntryTree компонент
   - Breadcrumbs: "Напитки > Газированные > Кола"
   - Защита от циклов

7. **Fuzzy search** — нечёткий поиск
   - "Snickers" ≈ "Сникерс"
   - Trigram индексы PostgreSQL
   - Highlighting результатов

8. **Entry versioning** — версионирование записей
   - valid_from / valid_to
   - Audit log всех изменений
   - Возможность отката

### Из UI_UX_SPECIFICATION:

9. **Два типа товаров** — принципиально разная логика
   - 🍵 Напитки: рецептура → себестоимость = Σ ингредиентов
   - 🍫 Снеки: перепродажа → закупка + наценка %
   - Автоматический расчёт маржинальности

10. **3-уровневый инвентарь** — трёхступенчатый учёт
    - Склад → Оператор → Машина
    - Отслеживание перемещений
    - Инвентаризация на каждом уровне

11. **AI Import с 7 агентами** — умный импорт файлов
    - Document Classification Agent
    - Schema Mapping Agent
    - Data Extraction Agent
    - Validation Agent
    - Reference Resolution Agent (fuzzy matching)
    - Reconciliation Agent
    - Learning Agent (улучшение со временем)

12. **Иерархическое заполнение БД** — 6 уровней
    - L0: Системные (seed)
    - L1: Справочники
    - L2: Основные сущности
    - L3: Структурные
    - L4: Операционные
    - L5: Аналитика

13. **Платёжные системы** — настраиваемые интеграции
    - Payme, Click, Uzum, TG Stars, Наличные
    - Webhook URL генерация
    - Test Mode toggle
    - Комиссии и лимиты

14. **AI Провайдеры** — мультипровайдерность
    - OpenAI, Claude, Gemini, Local LLM (Ollama)
    - Usage tracking и лимиты
    - Выбор провайдера для разных задач

15. **Конструктор внешних API** — no-code интеграции
    - Endpoint builder
    - Маппинг данных
    - Триггеры (event, cron, webhook)

### Из VENDHUB_UI_DOCUMENTATION_COMPLETE:

16. **Kanban для задач** — визуальное управление
    - Колонки: TODO → IN_PROGRESS → REVIEW → DONE
    - Drag-and-drop между колонками
    - Фильтры по оператору, машине, типу

17. **Photo reports** — Before/After фото
    - Обязательное фото до и после
    - Side-by-side сравнение
    - Approve/Reject от менеджера

18. **Real-time карта** — состояние автоматов
    - Cluster view для масштаба
    - Цветовые индикаторы статуса
    - Click → Quick view машины

19. **Telegram Bot команды** — 11 команд
    - /start, /help, /status, /tasks, /inventory
    - /report, /incidents, /schedule, /location
    - /settings, /cart
    - 45+ callback handlers

20. **Multi-tenant** — организационная структура
    - HQ → Organizations → Locations
    - Наследование прав
    - Scope справочников

### Из Appendix_F (React Frontend):

21. **Zustand stores** — клиентское состояние
    - directoriesStore — кэш справочников
    - entriesStore — записи с пагинацией
    - offlineStore — очередь offline операций

22. **React hooks** — переиспользуемая логика
    - useDirectory(slug) — метаданные справочника
    - useEntries(slug, filters) — записи с поиском
    - useSearch(slug, query) — debounced поиск
    - useLocalized(entry) — мультиязычность

23. **Optimistic updates** — мгновенный UI
    - Показ изменения до ответа сервера
    - Rollback при ошибке
    - Pending indicator

24. **Offline режим** — работа без сети
    - IndexedDB для справочников
    - Queue операций
    - Sync при восстановлении связи
    - Conflict resolution UI

### Из Appendix_C (Diagrams):

25. **Entry status workflow** — жизненный цикл записи
    ```
    DRAFT → PENDING_APPROVAL → ACTIVE → DEPRECATED → ARCHIVED
    ```

26. **RBAC resolution** — алгоритм прав
    - User permission (deny > allow)
    - Role permission
    - Directory default
    - Inherit from parent org

27. **Webhook delivery** — гарантированная доставка
    - HMAC-SHA256 signature
    - Exponential backoff retry
    - Dead letter queue

28. **Search ranking** — приоритет результатов
    - Exact match (weight: 1.0)
    - Prefix match (0.8)
    - Fuzzy match (0.5)
    - Recent selection boost (+0.3)

### Из Design System "Warm Brew":

29. **oklch цвета** — современная цветовая модель
    - Perceptually uniform
    - Accessible контрасты
    - Dark theme через oklch lightness

30. **Responsive sidebar** — адаптивная навигация
    - < 768px: Hidden (Sheet)
    - 768-1024px: Collapsed (icons only)
    - \> 1024px: Expanded (full)

### Из Appendix_A (SQL Migration):

31. **13 enum types** — все статусы системы для UI форм/фильтров
    ```
    directory_type:   MANUAL | EXTERNAL | PARAM | TEMPLATE
    directory_scope:  HQ | ORGANIZATION | LOCATION
    field_type:       TEXT | NUMBER | DATE | DATETIME | BOOLEAN |
                      SELECT_SINGLE | SELECT_MULTI | REF | JSON | FILE | IMAGE
    entry_origin:     OFFICIAL | LOCAL
    entry_status:     DRAFT | PENDING_APPROVAL | ACTIVE | DEPRECATED | ARCHIVED
    source_type:      URL | API | FILE | TEXT
    sync_status:      SUCCESS | FAILED | PARTIAL
    audit_action:     CREATE | UPDATE | ARCHIVE | RESTORE | SYNC | APPROVE | REJECT
    event_type:       ENTRY_CREATED | ENTRY_UPDATED | ENTRY_ARCHIVED | ENTRY_RESTORED |
                      SYNC_STARTED | SYNC_COMPLETED | SYNC_FAILED |
                      IMPORT_STARTED | IMPORT_COMPLETED | IMPORT_FAILED
    delivery_status:  PENDING | SUCCESS | FAILED | DEAD
    import_status:    PENDING | PROCESSING | COMPLETED | PARTIAL | FAILED | CANCELLED
    import_mode:      CREATE_ONLY | UPSERT | UPDATE_ONLY | DRY_RUN
    ```

32. **15 таблиц Master Data** — полная схема БД
    - directories, directory_fields, directory_entries
    - directory_sources, directory_sync_logs
    - directory_entry_audit, directory_permissions
    - directory_events, directory_stats
    - webhooks, webhook_deliveries, webhook_dead_letters
    - import_jobs, import_templates
    - user_recent_selections

33. **PostgreSQL функции** — автоматическая логика
    - `normalize_entry_name()` — lower + trim + unaccent
    - `check_hierarchy_cycle()` — защита от циклов
    - `update_updated_at()` — auto timestamps
    - `trg_update_entry_search_vector` — FTS индексация
    - `cleanup_recent_selections()` — очистка старых

### Из Appendix_B (JSONB Examples):

34. **validation_rules** — 8 типов валидации полей
    ```json
    // Regex с сообщением
    {"regex": "^[A-Z]{2}\\d{6}$", "custom_message": "Формат XX000000"}

    // Диапазон значений
    {"min_value": 0, "max_value": 999999999}

    // Уникальность в scope
    {"unique_scope": "DIRECTORY" | "ORGANIZATION" | "GLOBAL"}

    // Допустимые значения
    {"allowed_values": ["NEW", "USED", "REFURBISHED"]}

    // Conditional rules (кросс-поле)
    {"conditional_rules": [
      {"if": {"field": "type", "equals": "DRINK"},
       "then": {"field": "volume", "required": true}}
    ]}

    // Async validator с rate limit
    {"async_validator": "check_ikpu_exists",
     "rate_limit": {"requests_per_second": 10, "batch_size": 100}}
    ```

35. **directory.settings варианты** — 4 конфигурации
    ```json
    // Базовая
    {"allow_inline_create": true, "allow_local_overlay": true,
     "approval_required": false, "prefetch": false, "offline_enabled": false}

    // С prefetch (единицы измерения)
    {"prefetch": true, "offline_enabled": true, "offline_max_entries": 100}

    // С approval workflow
    {"approval_required": true, "approval_roles": ["manager", "admin"]}

    // EXTERNAL (ИКПУ)
    {"allow_inline_create": false, "allow_local_overlay": true,
     "allow_select_deprecated": false, "show_deprecated_warning": true}
    ```

36. **auth_config для EXTERNAL** — 5 типов аутентификации
    - Bearer Token: `{"type": "bearer", "token": "..."}`
    - Basic Auth: `{"type": "basic", "username": "...", "password": "..."}`
    - API Key Header: `{"type": "api_key", "header": "X-API-Key", "value": "..."}`
    - API Key Query: `{"type": "api_key_query", "param": "apikey", "value": "..."}`
    - OAuth2: `{"type": "oauth2_client", "token_url": "...", "client_id": "...", "scope": "..."}`

37. **entry.data полные примеры** — карточки сущностей
    ```json
    // Товар
    {"category_id": "uuid", "price": 5000, "cost_price": 3500, "vat_rate": 12,
     "ikpu_code": "17101001001001", "barcode": "4607123456789",
     "volume": 0.5, "shelf_life_days": 180,
     "storage_temperature": {"min": 2, "max": 25},
     "photo": {"file_id": "...", "url": "...", "thumb": "..."}}

    // Автомат
    {"machine_type_id": "uuid", "location_id": "uuid",
     "serial_number": "VM2024-001234", "manufacturer": "Necta", "model": "Krea Touch",
     "slots_count": 8, "payment_methods": ["cash", "card", "qr"],
     "has_telemetry": true, "telemetry_id": "TLM-001234"}

    // Локация
    {"address": "г. Ташкент, ул. Навои, 100", "contractor_id": "uuid",
     "coordinates": {"lat": 41.311081, "lng": 69.240562},
     "working_hours": {"monday": {"open": "09:00", "close": "18:00"}, ...},
     "rent_amount": 500000, "contract_number": "АР-2024/001"}

    // Контрагент
    {"legal_name": "ООО «Партнёр»", "inn": "123456789",
     "bank_mfo": "00450", "bank_account": "20208000123456789012",
     "is_supplier": true, "is_landlord": true, "payment_terms_days": 30}
    ```

38. **import preview_data структура** — для Preview шага
    ```json
    [
      {"row": 2, "values": {"A": "Coca-Cola"}, "mapped": {"name": "Coca-Cola"},
       "action": "CREATE", "status": "OK"},
      {"row": 3, "values": {"A": "Pepsi"}, "mapped": {"name": "Pepsi"},
       "action": "UPDATE", "status": "OK", "existing_id": "uuid"},
      {"row": 4, "values": {"A": ""}, "mapped": {"name": ""},
       "action": null, "status": "ERROR",
       "errors": [{"field": "name", "message": "Required field is empty"}]}
    ]
    ```

39. **Webhook payload** — полная структура события
    ```json
    {"id": "evt_uuid", "event_type": "ENTRY_UPDATED", "timestamp": "...",
     "directory": {"id": "...", "slug": "goods", "name": "Товары"},
     "entry": {"id": "...", "name": "...", "origin": "LOCAL", "version": 3},
     "changes": {"data.price": {"old": 4500, "new": 5000}},
     "actor": {"id": "...", "name": "Иванов И.И.", "email": "..."},
     "organization": {"id": "...", "name": "Головной офис"}}
    ```

### Из Appendix_D (Checklist):

40. **Frontend компоненты** — полный список UI
    - DirectoryList, DirectoryCard, DirectoryWizard
    - FieldBuilder (drag-and-drop + live preview)
    - EntryList (table), EntryTree (tree), EntryCard, EntryForm
    - EntryAuditLog — история изменений записи
    - DirectorySelect — autocomplete с группировкой OFFICIAL/LOCAL
    - InlineCreateForm — mini-form в dropdown
    - ImportWizard, ImportMapping, ImportPreview, ImportResult
    - SyncStatus — статус синхронизации EXTERNAL
    - PermissionsEditor — матрица ролей × действий
    - ConflictResolver — разрешение конфликтов offline

41. **Frontend Routes** — все маршруты справочников
    ```
    /directories                    — список справочников
    /directories/new                — создание (Wizard 6 шагов)
    /directories/:id                — просмотр справочника
    /directories/:id/edit           — редактирование
    /directories/:id/entries        — записи справочника
    /directories/:id/entries/:eid   — карточка записи
    /directories/:id/import         — импорт
    /directories/:id/sync           — синхронизация
    /directories/:id/permissions    — права доступа
    ```

42. **Backend модули NestJS** — 12 модулей
    - DirectoriesModule, EntriesModule, FieldsModule
    - SourcesModule, SyncModule, ImportModule
    - PermissionsModule, EventsModule, WebhooksModule
    - SearchModule, AuditModule, StatsModule

43. **Backend services** — 12 сервисов
    - DirectoryService, EntryService, SearchService
    - SyncService, ImportService, ExportService
    - PermissionService, EventService, WebhookService
    - AuditService, CacheService, ValidationService

44. **Testing requirements** — что тестировать
    - Unit: validators, normalize_entry_name, check_hierarchy_cycle, RBAC resolution
    - Integration: CRUD, Search (FTS + trigram), Inline create, Bulk import, Webhook delivery
    - E2E: Create directory wizard, Add entries, Search & select, Import from Excel
    - Performance: Search <500ms на 100k+, Import 10k rows <60s, Tree 1000 nodes <2s
    - Security: RBAC bypass, SQL injection, XSS in names

### Из Appendix_E (NestJS Backend):

45. **REST API endpoints** — структура
    ```
    GET    /api/directories                      — список
    POST   /api/directories                      — создать
    GET    /api/directories/:id                  — получить
    PATCH  /api/directories/:id                  — обновить
    DELETE /api/directories/:id                  — удалить (soft)

    GET    /api/directories/:id/entries          — записи
    POST   /api/directories/:id/entries          — создать запись
    GET    /api/directories/:id/entries/:eid     — получить запись
    PATCH  /api/directories/:id/entries/:eid     — обновить
    DELETE /api/directories/:id/entries/:eid     — архивировать
    POST   /api/directories/:id/entries/:eid/restore — восстановить

    GET    /api/directories/:id/entries/search   — поиск
    POST   /api/directories/:id/entries/bulk-import  — массовый импорт
    POST   /api/directories/:id/entries/bulk-update  — массовое обновление
    GET    /api/directories/:id/entries/export   — экспорт

    POST   /api/directories/:id/sync             — запустить синхронизацию
    GET    /api/directories/:id/sync-logs        — логи синхронизации
    ```

46. **Guards & Interceptors**
    - RbacGuard — проверка прав доступа
    - AuditInterceptor — автоматическая запись в audit log
    - CacheInterceptor — HTTP кэширование (ETag)

47. **Background Jobs (BullMQ)**
    - SyncScheduler — запуск синхронизации по cron
    - WebhookWorker — доставка webhooks с exponential backoff retry
    - CleanupJob — очистка old data (recent selections, dead letters)
    - StatsJob — обновление directory_stats

### Из Appendix_F (React Frontend) — дополнительно:

48. **API Client структура**
    ```typescript
    directoryApi.getDirectories(filters)
    directoryApi.getDirectoryBySlug(slug)
    directoryApi.createEntry(directoryId, dto)
    directoryApi.searchEntries(directoryId, {q, include_recent: true})
    directoryApi.bulkImport(directoryId, file, mapping)
    ```

49. **DirectorySelect states** — все состояния компонента
    - Closed — кнопка с выбранным значением
    - Open/Empty — показ recent selections
    - Open/Searching — spinner + debounced
    - Open/Results — группировка OFFICIAL/LOCAL
    - Open/NoResults — "Ничего не найдено" + Inline Create
    - Open/Creating — inline form

50. **ImportWizard 4 шага**
    ```
    Step 1: Upload      — FileDropzone (xlsx, csv, json)
    Step 2: Mapping     — ColumnMapper с auto-detection
    Step 3: Preview     — 10 rows + validation errors
    Step 4: Processing  — ProgressBar + results
    ```

### Из VendingPro System Documentation:

51. **Модуль Оборудование (Equipment)** — детальный учёт компонентов
    - **Бункеры (Bunkers)**: capacity, currentLevel, ingredientId, lastRefillDate
    - **Кофемолки (Grinders)**: grindLevel (1-10), totalGrinds, lastMaintenance
    - **Ёмкости для отходов (Waste Bins)**: capacity, currentLevel, lastEmptied
    - **Камеры (Cameras)**: streamUrl, isActive, lastCapture
    - **SIM-карты**: operator, phoneNumber, dataUsage, expiryDate

52. **Типы перемещений товаров** — 5 видов движений
    ```
    INCOMING      — приёмка от поставщика на склад
    REFILL        — загрузка со склада оператора в автомат
    WRITEOFF      — списание (истёк срок, брак, кража)
    TRANSFER      — перемещение между локациями
    BUNKER_MOVE   — перемещение ингредиента между бункерами
    ```

53. **История варок (Brew History)** — телеметрия напитков
    ```json
    {
      "machineId": "uuid",
      "productId": "uuid",
      "timestamp": "2025-01-26T10:30:00Z",
      "duration": 45,
      "telemetry": {
        "waterTemp": 92.5,
        "coffeeDose": 8.2,
        "milkDose": 150,
        "sugarDose": 5,
        "pressure": 9.2
      },
      "paymentMethod": "card",
      "transactionId": "txn_uuid"
    }
    ```

54. **Боковая навигация VendingPro** — модульная структура
    ```
    📚 Справочники
    ├── Товары
    ├── Ингредиенты
    ├── Категории
    └── Единицы измерения

    🏭 Оборудование
    ├── Автоматы
    ├── Типы автоматов
    ├── Бункеры
    ├── Кофемолки
    ├── Ёмкости для отходов
    ├── Камеры
    └── SIM-карты

    📦 Склад
    ├── Остатки
    ├── Приёмка
    └── Инвентаризация

    🔄 Движения
    ├── Все перемещения
    ├── Загрузки
    ├── Списания
    └── Переносы

    ☕ Варки
    ├── История
    ├── Статистика
    └── Телеметрия
    ```

55. **Карточка автомата — расширенные вкладки**
    - Info: основные данные + geo + photo
    - Bunkers: 3D визуализация уровней бункеров
    - Slots: планограмма с ценами и остатками
    - Telemetry: real-time графики (температура, давление)
    - History: варки + инциденты + ТО
    - Cameras: live stream + последние кадры

### Из Universal Catalog Documentation:

56. **6 типов полей каталога** — универсальная система
    ```
    TEXT          — произвольный текст: артикулы, описания
    NUMBER        — числовые значения: цены, количества
    BOOLEAN       — логический флаг: органик, критичность
    SELECT        — выбор из списка: единицы, страны, уровни
    RELATION      — ссылка на другой справочник: бренд, поставщик
    RELATION_MULTI — несколько ссылок: совместимые автоматы
    ```

57. **Система представлений (Views)** — настраиваемые таблицы
    - Выбор видимых колонок
    - Сортировка по любому полю (ASC/DESC)
    - Встроенные фильтры (автофильтр при открытии)
    - Drag & Drop колонок для изменения порядка
    - Сохранение настроек в View

    **Примеры:**
    - «Все» — полный список со всеми полями
    - «Закупка» — только название, артикул, цена, мин. остаток
    - «Органик» — автофильтр по органическим продуктам
    - «Критичные» — запчасти с флагом критичности

58. **10 операторов фильтрации** — мощная система условий
    ```
    eq            =     Равно
    ne            ≠     Не равно
    gt / lt       > <   Больше / Меньше
    gte / lte     ≥ ≤   Больше или равно / Меньше или равно
    contains      ∈     Содержит подстроку
    notcontains   ∉     Не содержит подстроку
    empty         ∅     Пусто
    notempty      ∅̷     Не пусто
    ```

    **Логика объединения:**
    - AND (все) — запись соответствует ВСЕМ условиям
    - OR (любое) — запись соответствует ЛЮБОМУ условию

    **Сохранённые фильтры:**
    - «Дорогие позиции» (цена > 1000)
    - «Мало на складе» (мин. остаток < 5)

59. **Drag & Drop сортировка** — ручное упорядочивание
    - **Строки:** иконка захвата (⋮⋮) слева от чекбокса
    - **Колонки:** перетаскивание заголовков столбцов
    - Автоматическое сохранение порядка
    - Работает в пределах одного уровня иерархии

60. **CSV импорт/экспорт** — обмен данными
    **Импорт:**
    - Формат: CSV с разделителем ";"
    - Обязательная колонка: «Название» или «name»
    - Автоматическое сопоставление заголовков с полями
    - Предпросмотр первых 5 строк перед импортом
    - Автоконвертация типов: числа, булевы (да/true/1)

    **Экспорт:**
    - Только видимые колонки текущего представления
    - UTF-8 с BOM для корректного открытия в Excel
    - Имя файла из названия группы

61. **Вложенные сущности (Hierarchical Entries)** — иерархия внутри записей
    - Любая запись может содержать дочерние записи
    - Неограниченная глубина вложенности
    - Сворачивание/разворачивание визуально
    - Визуальный отступ для дочерних записей

    **Пример:** `Помпа ULKA EP5 → Ремкомплект помпы, Уплотнитель помпы`

62. **Inline-редактирование ячеек** — быстрое изменение
    - Двойной клик по ячейке → режим редактирования
    - Автосохранение при потере фокуса или Enter
    - Escape для отмены изменений
    - Валидация в реальном времени

63. **Карточка записи (Entry Card)** — боковая панель
    - Полная информация о записи
    - Режимы: просмотр / редактирование
    - История изменений (audit log)
    - Связанные записи (relations)
    - Создание вложенной сущности через кнопку «Вложенная»

64. **Горячие клавиши** — быстрая работа
    ```
    Escape      — закрыть модальное окно / карточку
    ⌘K / Ctrl+K — фокус на поиск
    ⌘N / Ctrl+N — создать новую запись
    ```

65. **Конструктор полей (Field Builder)** — настройка структуры
    - Создание: название, ключ, тип, область видимости
    - Редактирование параметров существующих полей
    - Удаление неиспользуемых полей
    - Варианты для SELECT: ввод через запятую
    - Связи: выбор целевого справочника
    - Флаг обязательности поля

---

## 📋 ЧЕКЛИСТ ПОЛНОТЫ ДОКУМЕНТА

При генерации финальной структуры убедись, что включены:

### Базовая структура:
- [ ] Все 4 интерфейса (Admin, Staff, Client, Investor)
- [ ] 80+ экранов Admin Dashboard с путями и назначением
- [ ] Master Data Management как сквозной модуль
- [ ] Directory Builder Wizard (6 шагов) детально
- [ ] RBAC (7 ролей) с матрицей доступа

### Справочники и данные:
- [ ] DirectorySelect component со всеми состояниями
- [ ] OFFICIAL/LOCAL маркировка везде
- [ ] Inline Create механика
- [ ] Recent selections
- [ ] 6 типов полей каталога (text, number, boolean, select, relation, relation-multi)
- [ ] 10 операторов фильтрации с AND/OR логикой
- [ ] Система представлений (Views) с настройкой колонок
- [ ] Вложенные сущности (иерархические записи)
- [ ] Конструктор полей (Field Builder)

### Товары и склад:
- [ ] 2 типа товаров (напитки с рецептурой, снеки с наценкой)
- [ ] 3-уровневый инвентарь (склад → оператор → машина)
- [ ] 5 типов перемещений товаров (incoming, refill, writeoff, transfer, bunkerMove)

### Оборудование:
- [ ] Модуль оборудования: бункеры, кофемолки, ёмкости для отходов, камеры, SIM
- [ ] Карточка автомата с расширенными вкладками (Bunkers, Telemetry, Cameras)
- [ ] История варок с телеметрией (waterTemp, coffeeDose, pressure)

### Операции и UI:
- [ ] Kanban задач
- [ ] Photo reports (before/after)
- [ ] Real-time карта с cluster view
- [ ] Drag & Drop сортировка строк и колонок
- [ ] Inline-редактирование ячеек
- [ ] Карточка записи (боковая панель)
- [ ] Горячие клавиши (⌘K, ⌘N, Escape)

### Импорт/Экспорт:
- [ ] AI Import с 7 агентами
- [ ] CSV импорт с автосопоставлением колонок
- [ ] CSV экспорт видимых колонок (UTF-8 BOM)
- [ ] ImportWizard (4 шага: Upload → Mapping → Preview → Process)

### Интеграции и коммуникации:
- [ ] Telegram Bot (11 команд)
- [ ] Webhooks и Events
- [ ] Платёжные системы (Payme, Click, Uzum, TG Stars)
- [ ] Фискализация (MultiKassa, Честный знак)

### Технические аспекты:
- [ ] Offline режим для Staff App (IndexedDB + Queue)
- [ ] Design System "Warm Brew" (oklch, responsive sidebar)
- [ ] Zustand stores (directories, entries, offline)
- [ ] React hooks (useDirectory, useEntries, useSearch)
- [ ] 15 таблиц Master Data в PostgreSQL
- [ ] 13 enum types для статусов

---

**Конец промта v1.1**

*Обновлено: 26 января 2026*
*Добавлены пункты 51-65 из VendingPro и Universal Catalog документации*
