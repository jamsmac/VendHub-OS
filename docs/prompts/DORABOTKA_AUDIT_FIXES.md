# 🔧 Промпт на Доработку VendHub OS по Результатам Аудита

**Для:** Claude Code Agent
**Дата аудита:** 4 февраля 2025
**Текущая готовность:** 88/100
**Целевая готовность:** 95/100+

---

## 🤖 Инструкции для Claude Code

### Доступные Инструменты

Ты работаешь как Claude Code агент с полным доступом к следующим инструментам:

**Файловые операции:**
- `Read` - чтение файлов
- `Write` - создание файлов
- `Edit` - редактирование файлов
- `Glob` - поиск файлов по паттерну
- `Grep` - поиск текста в файлах

**Выполнение команд:**
- `Bash` - выполнение shell команд (npm, pnpm, git, tsc, etc.)

**Агентные возможности:**
- `Task` - запуск параллельных субагентов для сложных задач
- `TodoWrite` - отслеживание прогресса выполнения

**Веб-ресурсы:**
- `WebSearch` - поиск актуальной документации
- `WebFetch` - получение контента веб-страниц

---

### 🎯 Доступные VendHub Скиллы

Используй специализированные скиллы для VendHub OS:

| Скилл | Когда использовать |
|-------|-------------------|
| `vhm24-orchestrator` | Координация комплексных задач, планирование |
| `vhm24-ui-generator` | Генерация React/TypeScript экранов |
| `vhm24-api-generator` | Создание tRPC API роутов |
| `vhm24-db-expert` | Работа с Drizzle ORM, схемами БД |
| `vhm24-qa-review` | Code review, проверка качества |
| `vhm24-testing` | Написание тестов (unit, integration, e2e) |
| `vhm24-component-lib` | Создание shadcn/ui компонентов |
| `vhm24-forms` | Сложные формы с React Hook Form + Zod |
| `vhm24-i18n` | Локализация, переводы |
| `vhm24-auth-rbac` | Авторизация, роли, права доступа |
| `vhm24-charts` | Графики, дашборды с Recharts |
| `vhm24-realtime` | WebSocket, real-time функционал |
| `vhm24-mobile` | Telegram WebApp, мобильные паттерны |
| `vhm24-payments` | Платёжные системы Узбекистана |

---

## 📋 Контекст Проекта

**Структура монорепо:**
```
VendHub OS/
├── apps/
│   ├── api/      # NestJS 11 + TypeORM + PostgreSQL
│   ├── web/      # Next.js 15 + React 19 + shadcn/ui
│   ├── client/   # React 18 + Vite (Telegram WebApp)
│   ├── bot/      # Telegraf Telegram Bot
│   └── mobile/   # React Native
├── packages/
│   ├── shared/   # Общие типы и утилиты
│   └── ui/       # shadcn/ui компоненты
└── docs/         # Документация
```

**Технологический стек:**
- TypeScript strict mode
- pnpm workspaces + Turborepo
- tRPC для типобезопасного API
- Zod для валидации
- TanStack Query для data fetching
- Zustand для state management

---

## 🎯 Задачи на Доработку

### ФАЗА 1: Критические TypeScript Ошибки

#### 1.1 🔴 Web App - Fiscal Page (15 ошибок)

**Проблема:** `apps/web/src/app/dashboard/fiscal/page.tsx`

**Алгоритм исправления:**

```
1. Используй Grep для поиска всех ошибок:
   Grep: pattern="error TS" path="apps/web"

2. Прочитай файл fiscal/page.tsx:
   Read: apps/web/src/app/dashboard/fiscal/page.tsx

3. Прочитай index.ts экспорты:
   Read: apps/web/src/lib/api/index.ts

4. Проверь что fiscalApi экспортируется:
   - Если нет - добавь: export { fiscalApi } from './fiscal.api';

5. Исправь типы unknown -> конкретные типы:
   - devices: FiscalDevice[]
   - data: соответствующий тип из @/types/fiscal.types

6. Добавь типы параметрам функций:
   - (d) -> (d: FiscalDevice)
   - (r) -> (r: FiscalReceipt)
   - (receipt) -> (receipt: FiscalReceipt)

7. Проверь результат:
   Bash: cd apps/web && npx tsc --noEmit 2>&1 | grep -c "error"
```

**Используй скилл:** `vhm24-qa-review` для финальной проверки

---

#### 1.2 🔴 Client App - i18n Типизация (3 ошибки)

**Файлы:**
- `apps/client/src/pages/LoyaltyPage.tsx`
- `apps/client/src/pages/QuestsPage.tsx`

**Алгоритм исправления:**

```
1. Найди файлы с ошибками:
   Grep: pattern="pointsRemaining|fromPoints|pointsReceived" path="apps/client"

2. Прочитай типы i18n:
   Read: apps/client/src/i18n/types.ts (если есть)
   Read: apps/client/src/i18n/index.ts

3. Определи проблему:
   - count ожидает number, передаётся string

4. Исправь передачу параметров:
   Edit: t('pointsRemaining', { count: Number(value) })

5. ИЛИ обнови типы i18n если нужна поддержка string

6. Проверь результат:
   Bash: cd apps/client && npx tsc --noEmit 2>&1 | grep -c "error"
```

**Используй скилл:** `vhm24-i18n` для правильных паттернов локализации

---

### ФАЗА 2: Тесты и Безопасность

#### 2.1 🟡 API Тесты (55 ошибок в .spec.ts)

**Алгоритм:**

```
1. Получи список файлов с ошибками:
   Bash: cd apps/api && npx tsc --noEmit 2>&1 | grep "\.spec\.ts" | cut -d'(' -f1 | sort -u

2. Для каждого файла:
   - Read файл
   - Определи тип ошибки (моки, типы, импорты)
   - Edit исправления

3. Типичные исправления:
   - jest.mock() -> добавить типы
   - expect().toHaveBeenCalledWith() -> правильные аргументы
   - Устаревшие импорты -> обновить пути

4. Запусти тесты:
   Bash: cd apps/api && pnpm test --passWithNoTests
```

**Используй скилл:** `vhm24-testing` для паттернов тестирования

**Используй Task** для параллельного исправления нескольких файлов:
```
Task: subagent_type="general-purpose"
      prompt="Исправь TypeScript ошибки в файле X.spec.ts"
```

---

#### 2.2 🟡 Security Audit

**Алгоритм:**

```
1. Проверь уязвимости:
   Bash: cd /path/to/project && pnpm audit --json | head -100

2. Если есть критические:
   Bash: pnpm audit fix

3. Проверь CORS настройки:
   Read: apps/api/src/main.ts
   - Убедись что origin настроен для production

4. Проверь Helmet:
   Grep: pattern="helmet" path="apps/api/src"

5. Проверь rate limiting:
   Grep: pattern="ThrottlerModule|throttler" path="apps/api/src"

6. Добавь CSP в Next.js если отсутствует:
   Read: apps/web/next.config.js
   Edit: добавь securityHeaders
```

---

#### 2.3 🟡 CORS Configuration

**Файл:** `apps/api/src/main.ts`

```typescript
// Найди и обнови или добавь:
app.enableCors({
  origin: process.env.CORS_ORIGINS?.split(',') || [
    'https://vendhub.uz',
    'https://admin.vendhub.uz',
    'https://app.vendhub.uz',
  ],
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization', 'X-Organization-Id'],
});
```

---

### ФАЗА 3: Оптимизация и Cleanup

#### 3.1 🟢 Очистка Build Артефактов

```bash
# Выполни через Bash:
find . -type d -name ".turbo" -exec rm -rf {} + 2>/dev/null
find . -type d -name ".next" -exec rm -rf {} + 2>/dev/null
find . -name "*.log" -not -path "./node_modules/*" -delete 2>/dev/null
```

#### 3.2 🟢 Pre-commit Hooks

```bash
# Установи и настрой:
pnpm add -D husky lint-staged
npx husky init
```

**Создай файл `.husky/pre-commit`:**
```bash
#!/bin/sh
pnpm lint-staged
pnpm type-check
```

**Добавь в `package.json`:**
```json
"lint-staged": {
  "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
  "*.{json,md}": ["prettier --write"]
}
```

---

## 🔄 Стратегия Выполнения

### Рекомендуемый Подход

1. **Используй TodoWrite** для отслеживания всех задач

2. **Запускай параллельные Task** для независимых задач:
   ```
   Task 1: Исправление Web TypeScript ошибок
   Task 2: Исправление Client i18n ошибок
   Task 3: Security audit
   ```

3. **Используй скиллы** для специфичных задач:
   - `/vhm24-qa-review` после каждого крупного исправления
   - `/vhm24-testing` для написания недостающих тестов
   - `/vhm24-i18n` для локализации

4. **Верифицируй каждый шаг:**
   ```bash
   # После каждого исправления:
   pnpm type-check
   pnpm lint
   pnpm test
   ```

---

## ✅ Критерии Завершения

Выполни финальную проверку:

```bash
# 1. TypeScript - 0 ошибок
cd apps/api && npx tsc --noEmit && echo "API: OK"
cd apps/web && npx tsc --noEmit && echo "Web: OK"
cd apps/client && npx tsc --noEmit && echo "Client: OK"
cd apps/bot && npx tsc --noEmit && echo "Bot: OK"

# 2. Линтер - 0 ошибок
pnpm lint

# 3. Тесты - все проходят
pnpm test

# 4. Билд - успешный
pnpm build

# 5. Audit - 0 критических
pnpm audit
```

---

## 📊 Отчёт о Выполнении

После завершения создай отчёт:

```markdown
# Отчёт о Доработке VendHub OS

## Выполненные Задачи
- [ ] Исправлено X TypeScript ошибок в Web
- [ ] Исправлено Y ошибок i18n в Client
- [ ] Исправлено Z ошибок в тестах API
- [ ] Проведён security audit
- [ ] Настроен CORS
- [ ] Добавлены pre-commit hooks

## Метрики До/После
| Метрика | Было | Стало |
|---------|------|-------|
| TypeScript ошибок | 73 | 0 |
| Готовность | 88% | 95%+ |

## Рекомендации
...
```

---

## 📎 Связанные Документы

- `docs/COMPREHENSIVE_AUDIT_2025_02_04.md` - Результаты аудита
- `docs/architecture/` - Архитектурная документация
- `docs/api/` - API документация

---

## 💡 Советы для Claude Code

1. **Всегда читай файл перед редактированием** - используй Read перед Edit

2. **Используй Grep для поиска паттернов** вместо ручного поиска

3. **Запускай проверки после каждого изменения:**
   ```bash
   npx tsc --noEmit 2>&1 | grep -c "error"
   ```

4. **Для больших задач используй Task** с subagent_type="general-purpose"

5. **Документируй изменения** в git commits:
   ```bash
   git add -A && git commit -m "fix(web): resolve TypeScript errors in fiscal page"
   ```

6. **При ошибках используй WebSearch** для поиска решений:
   ```
   WebSearch: "TypeScript error TS2305 Module has no exported member"
   ```

---

*Промпт оптимизирован для Claude Code Agent с полным использованием доступных инструментов и скиллов VendHub OS.*
