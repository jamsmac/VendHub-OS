# PNPM Configuration for VendHub Monorepo

# Allow React Native dependencies to be hoisted properly
# This fixes the @types/react conflict between web and mobile apps
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=@types/*

# Ensure React Native apps get their own node_modules
node-linker=hoisted

# Shamefully hoist for compatibility with Expo
shamefully-hoist=true

# Auto-install peers
auto-install-peers=true
