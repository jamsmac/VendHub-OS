/**
 * Shared Enums — re-exported from @vendhub/shared (single source of truth).
 *
 * All 30+ consumer files import from this path (../../common/enums).
 * This file delegates to @vendhub/shared so enums are defined once.
 */
export {
  UserRole,
  PaymentMethod,
  CommissionType,
  ContractType,
  ContractStatus,
} from "@vendhub/shared";
