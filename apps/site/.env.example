# VendHub Site (Landing Page) Environment Variables
# Copy to .env.local and fill in values

# API URL for server-side requests
NEXT_PUBLIC_API_URL=http://localhost:4000

# Port (default 3100)
PORT=3100
